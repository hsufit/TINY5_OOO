cmake_minimum_required(VERSION 3.20)
project(tiny5 LANGUAGES CXX)
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)
include(CTest)
include(cmake/SystemCDependency.cmake)
add_subdirectory(riscv_emulator)
option(TINY5_BUILD_RTL "Build the Verilated SystemVerilog cores and tests" ON)
if(TINY5_BUILD_RTL)
    add_subdirectory(rtl)
endif()
